"""survey — the aut2ltl evaluation harness (a *client* of aut2ltl, not part of it).

Runs the engine end-to-end over corpora and compares/measures the results.
Subpackages provide the reusable pieces; `survey.diff` is the directional
language-comparison tooling (containment direction + witness words).
"""
